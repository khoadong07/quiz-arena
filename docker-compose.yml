services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    restart: always

  client:
    build: ./client
    ports:
      - "5173:5173"
    restart: always
    depends_on:
      - server
